#
# Specific settings for local development
#
settings:
  # Sets event.requestContext.stage to this value
  stage: 'local'

  # These are dummy values for when running on a development computer, to keep Serverless happy
  proxyHostName: 'tokenhandler.authsamples-local.com'
  certificateDomainName: 'authsamples-local.com'
