{
  "name": "reverseproxy",
  "version": "1.0.0",
  "description": "A Serverless reverse proxy to manage cookie routing in an API driven SPA architecture",
  "scripts": {
    "lint": "npx eslint . --ext .ts,.tsx",
    "build": "rimraf dist && tsc --sourceMap true",
    "buildRelease": "rimraf dist && tsc",
    "setup": "./test/setup.sh",
    "http": "./test/runHttpWorkflow.sh",
    "lambda": "./test/runLambdaWorkflow.sh",
    "packageLocal": "npm run buildRelease && rimraf .serverless && sls package --stage local",
    "deployLocal": "sls deploy --stage local --package .serverless",
    "removeLocal": "sls remove --stage local",
    "package": "npm run buildRelease && rimraf .serverless && sls package --stage deployed",
    "deploy": "sls deploy --stage deployed --package .serverless",
    "remove": "sls remove --stage deployed"
  },
  "author": "Gary Archer",
  "license": "MIT",
  "dependencies": {
    "@middy/core": "^2.5.3",
    "axios": "^0.26.0",
    "base64url": "^3.0.1",
    "cookie": "^0.4.1",
    "guid-typescript": "^1.0.9",
    "fs-extra": "^9.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.85",
    "@types/cookie": "^0.4.0",
    "@types/fs-extra": "^9.0.4",
    "@types/node": "^14.14.10",
    "@typescript-eslint/eslint-plugin": "^5.10.1",
    "@typescript-eslint/parser": "^5.10.1",
    "eslint": "^7.32.0",
    "proxy-agent": "^5.0.0",
    "rimraf": "^3.0.2",
    "serverless": "^3.0.1",
    "serverless-domain-manager": "4.1.0",
    "typescript": "4.5.5"
  }
}
